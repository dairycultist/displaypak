# displaypak
Electron/Tauri are bloat fr, I want LIGHTWEIGHT and SIMPLE package for rendering interactable UI elements to a window

The window is composed of a tree of "areas." An area is basically a flexbox (with a background color and padding) that can contain other areas, text, images/texture, etc.
